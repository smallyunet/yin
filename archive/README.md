# Historical material

This directory preserves implementation fragments that are not part of the
current compiler or runtime. The original prototype programs remain in
`prototype1/` and the exploratory Yin programs remain in `experiments/` so
their Git history and existing references stay easy to follow.
