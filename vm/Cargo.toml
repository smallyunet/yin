[package]
name = "yin-vm"
version = "0.22.0"
edition = "2024"
description = "Independent fuel-metered runtime for Yin portable decision bytecode"
license = "AGPL-3.0-only"

[[bin]]
name = "yinvm"
path = "src/main.rs"

[dependencies]
serde_json = { version = "1.0", features = ["preserve_order"] }
sha2 = "0.10"
