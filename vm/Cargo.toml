[package]
name = "yin-vm"
version = "0.19.0"
edition = "2021"
description = "Deterministic Rust runtime for portable Yin bytecode"
license = "AGPL-3.0-only"

[[bin]]
name = "yinvm"
path = "src/main.rs"

[dependencies]
serde_json = { version = "1.0", features = ["preserve_order"] }
sha2 = "0.10"
